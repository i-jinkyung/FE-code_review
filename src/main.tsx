import React from 'react'
import ReactDOM from 'react-dom/client'
import { CodeReview } from './index.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CodeReview />
  </React.StrictMode>,
)
